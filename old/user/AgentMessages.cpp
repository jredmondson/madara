#include <stdlib.h>

#include "AgentMessages.h"
//#include "AgentContext.h"


/*void Madara::fillPingFromContext (Madara::AgentPing & ping, 
                                  Madara::AgentContext & context)
{
  ping.size = sizeof (Madara::AgentPing) - sizeof (ping.size);
  ping.type = Madara::AGENT_PING;
  strcpy (ping.host, context.getHost ().c_str ());
  ping.port = atoi (context.getPort ().c_str ());
}*/