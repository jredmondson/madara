#include <stdlib.h>

#include "Agent_Messages.h"
//#include "Agent_Context.h"


/*void Madara::fillPingFromContext (Madara::Agent_Ping & ping, 
                                  Madara::Agent_Context & context)
{
  ping.size = sizeof (Madara::Agent_Ping) - sizeof (ping.size);
  ping.type = Madara::AGENT_PING;
  strcpy (ping.host, context.getHost ().c_str ());
  ping.port = atoi (context.getPort ().c_str ());
}*/