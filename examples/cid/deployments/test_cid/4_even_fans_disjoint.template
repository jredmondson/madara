0 -> [0,size/4)                 // 0 sends to the first quarter
size/4 -> [size/4, size/2)    // size/4 sends to the second quarter
size/2 -> [size/2, 3*size/4)  // size/2 sends to the third quarter
3*size/4 -> [3*size/4, size)  // size*3/4 sends to last quarter
